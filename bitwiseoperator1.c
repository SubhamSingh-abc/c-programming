#include <stdio.h>         // & operator
int main (){               // & operator converts bit into 1 if both corresponding binary 
    int a = 2 ;            // are 1   
    int b = 0 ;  
    printf("%d\n",a&b);
    return 0;
}