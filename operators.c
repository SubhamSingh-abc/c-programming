// = → Assign
// a + b;   sum
// a - b;   difference
// a * b;   product
// a / b;   quotient
// a % b;    remainder
// a^=b = a^b
// != not equal to

// a++ → Post-increment (use, then increase)

// ++a → Pre-increment (increase, then use)

// a-- → Post-decrement (use, then decrease)

// --a → Pre-decrement (decrease, then use)

// a += b; → a = a + b;

// a -= b; → a = a - b;

// a *= b; → a = a * b;

// a /= b; → a = a / b;

// a %= b; → a = a % b;

// == → Equal

// != → Not equal

// > → Greater than

// < → Less than

// >= → Greater or equal

// <= → Less or equal

// && →  AND

// || →  OR

// ! → NOT

// << → Left shift

// >> → Right shift

// a &= b; → a = a & b;

// a |= b; → a = a | b;

// a ^= b; → a = a ^ b;

// a <<= 2; → a = a << 2;

// a >>= 2; → a = a >> 2;

